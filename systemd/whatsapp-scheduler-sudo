# Allow the scheduler to restart the driver without password
# Install with: sudo cp systemd/whatsapp-scheduler-sudo /etc/sudoers.d/whatsapp-scheduler
# Make sure to set permissions: sudo chmod 440 /etc/sudoers.d/whatsapp-scheduler

iedwards ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart whatsapp-driver
